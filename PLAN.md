# Canon — Codebase Review & Next Steps

## Status
Core pipeline complete and live-tested. README and DEVNOTES updated.
This document tracks the remaining work identified in the consistency review below.

---

## Consistency & Efficiency Review

### High — Should fix before shipping

| # | Issue | File(s) | Fix |
|---|---|---|---|
| H1 | `_extract_text` and `_parse_json_list` duplicated verbatim in both `topic_advisor.py` and `survey_designer.py` | `agent/topic_advisor.py`, `agent/survey_designer.py` | Move shared helpers to `agent/utils.py`, import from both |
| H2 | Integration test only covers the legacy `build_survey_params(topic_query)` path — the new `build_survey_params_from_topic(topic_id, n)` batch path has zero test coverage | `tests/test_integration_survey_designer.py` | Add tests for batch path; mock DB calls |
| H3 | `PLAN.md` schema (and old checklist) references stale design — `store/writer.py`, `store/history.db`, `store/topics/`, `store/designs/` — all superseded by current implementation | `PLAN.md` | Replace with current state (done in this file) |

### Medium — Worth addressing

| # | Issue | File(s) | Fix |
|---|---|---|---|
| M1 | `_DESIGN_PROMPT_BATCH_SUFFIX` is a module-level string constant with `<N>` placeholder — not obvious it needs `.replace()` before use; no guard if forgotten | `agent/survey_designer.py` | Fine as-is but add a one-line comment noting the `<N>` substitution requirement |
| M2 | `discovery_prompt.md` hardcodes `template_id: null` with the comment "no template API available" — this is now a known assumption but will silently produce null in every topic suggestion | `agent/prompts/discovery_prompt.md` | Remove the instruction; let the field be absent so it doesn't populate DB as null when there's no real value |
| M3 | `search_sessions()` in `harmonica/client.py` duplicates `list_sessions(keyword=query)` — two methods that hit the same endpoint with the same param | `harmonica/client.py` | Remove `search_sessions`, callers should use `list_sessions(keyword=...)` |
| M4 | `store/__init__.py` exports `export_vault` from `vault.py` but `main.py` imports `from store.vault import export_vault` directly — import path is inconsistent | `main.py`, `store/__init__.py` | Use `from store.vault import export_vault` everywhere (already the main.py pattern; the `__init__` re-export is unused) |
| M5 | `CANON_STORE_DIR` env var is supported in `db.py` but not documented in README, DEVNOTES, or `.env.example` | `store/db.py` | Add to env vars table in README and `.env.example` |

### Low — Polish

| # | Issue | File(s) | Fix |
|---|---|---|---|
| L1 | Walkthrough comment chain (lines like `# → next: agent/survey_designer.py:35`) has stale line numbers after all the refactoring | `agent/__init__.py`, `agent/survey_designer.py`, `agent/results_ingestor.py`, `harmonica/__init__.py`, `harmonica/client.py` | Strip `→ next:` breadcrumb comments — they've diverged and add noise |
| L2 | `requirements.txt` has no pinned versions beyond floor constraints — fine for dev but `pytest` is a dev dependency that shouldn't be in the prod install | `requirements.txt` | Split into `requirements.txt` (runtime) and `requirements-dev.txt` (pytest) |
| L3 | `store/vault.py` `_write_index` hardcodes `*Auto-generated by Canon*` — minor but "Canon" is the internal codename, not the product name | `store/vault.py` | Cosmetic; leave for now |

---

## Ordered Fix List

1. ✅ **H1** — Extracted `extract_text` / `parse_json` / `parse_json_list` to `agent/utils.py`; removed from both agent classes. Also fixed temp file issue — `_write_session_md_from_content` now uses `tempfile` instead of writing `session.md` to cwd.
2. ✅ **M3** — Removed `search_sessions()` from `harmonica/client.py`
3. ✅ **M2** — Removed `template_id: null` line from `discovery_prompt.md`
4. ✅ **M5** — Added `CANON_STORE_DIR` to README env table and `.env.example`
5. ✅ **L1** — Stripped all stale `→ next:` breadcrumb comments from all files
6. ✅ **M4** — Dropped unused `export_vault` re-export from `store/__init__.py`
7. ✅ **H2** — Added `TestBuildSurveyParamsFromTopic` to integration test suite; seeds a topic row via `db.insert_batch/insert_topic`, tests count, required keys, DB storage, and variation diff
8. ✅ **L2** — Split into `requirements.txt` (runtime) and `requirements-dev.txt` (pytest)

---

## Phase 7 — Streamlit Dashboard (future)

- `app.py` — Discover page: run TopicAdvisor, browse topics, select
- Design page: run SurveyDesigner, compare designs side-by-side, approve
- Sessions page: create session, show join URL, poll status
- History page: browse all stored topics/designs/sessions, render .md files

---

## Assumptions (carried forward, all confirmed)

| # | Assumption |
|---|---|
| 1 | `template_id` is an optional free string on `POST /api/v1/sessions`; no list endpoint exists |
| 2 | The Bonfires KG agent can return multiple suggestions as a JSON array in a single call |
| 3 | No-query discovery uses `kg.get_latest_episodes(agent_id, limit)` — `kg.search()` requires non-empty string |
| 4 | `store/canon.db` path is configurable via `CANON_STORE_DIR` env var |
| 5 | Batch designs are N variations on the same topic, not N independent topics |
| 6 | `--topic` legacy flag is kept for backwards compatibility, not promoted |
